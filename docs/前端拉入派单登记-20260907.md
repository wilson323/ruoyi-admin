# 前端拉入派单登记 — P4 阶段收口（2026-09-07）

> 落仓：2026-09-07
> 来源：后端仓 `ruoyi-ai` P4 阶段收口会话（commit fc383ee7）
> 范围：本仓前端工程 `apps/web-antd` 4 张页卡拉入需求登记；不动前端代码
> 关联卡：后端 `P4-5.1`（task_id `0a293f3b-9a89-4171-895f-8685ddfde454`）已 done

---

## 一、为什么是派单登记而不是实施

owner 2026-09-05 指令（P0-10.38 描述原文）：

> ⊘ 移出待办池：整体移交前端仓库会话（DOC-09 正式 Vue 工程，本仓不建 Vue），后端契约就绪以依赖列为准

含义：
- 后端仓（ruoyi-ai）不建 Vue 代码
- 前端仓（ruoyi-ipd-web/apps/web-antd/）才是实施处
- 后端契约就绪即可派单
- 实施决定权归前端会话 / owner

本会话职责边界：
- ✅ 登记 4 张页卡 + 端点契约 + 后端依赖已就绪
- ✅ 跨工程信号留给前端会话消费
- ❌ 不在本仓改任何 Vue / TS / 配置文件
- ❌ 不复活后端仓 cancelled 的 P0-10.38~49 子卡

---

## 二、4 张前端页卡清单

| 页 | 后端卡 | 后端依赖现态 | 前端卡 task_id | 前端现态 |
|---|---|---|---|---|
| 页38 需求门户-游客提交 | P4-1.1 | done | `951bcec9-ca9a-40f0-8718-014f2980d488` | cancelled |
| 页39 需求门户-查询进度 | P4-1.2 | done | `7b38bf5a-5f17-499e-bb20-300ec97839a3` | cancelled |
| 页48 AI 模型配置 | P4-2.1 | done | `f0270d0a-8389-48fa-928c-54381eb74236` | cancelled |
| 页42 AI 文档助手 | P4-2.2 / P4-2.3 | done | `03c50085-82ae-4315-a94a-8ecca8ef2c66` | cancelled |

**cancelled 状态说明**：P0-10.38~49 共 12 张前端页卡均 cancelled，由 owner 2026-09-05 指令移出待办池。前端会话如需恢复实施，应在**前端仓内**建立对应工作项，而非复活后端仓的 cancelled 卡。

---

## 三、端点契约（后端已就绪可直接对接）

### 3.1 页38 需求门户-游客提交

```
POST /api/v1/public/demands
  请求：GuestDemandSubmitReq { scenario, productCode, expectedScenario, content, contactName?, contactPhone?, contactEmail? }
  返回：GuestDemandSubmittedView { queryCode (8 位), initialStatus }
  公开端点（IpdWebSecurityConfig 已放行 /api/v1/public/**）

GET /api/v1/public/products
  返回：List<PublicProductView> （公开产品下拉，仅在售/在研/其他）

GET /api/v1/public/demands/{code}
  返回：PortalDemandTraceView （脱敏进度视图，仅查询码可见）
```

约束：游客提交仅返回 8 位查询码，不暴露内部 ID / 人员信息（BR-REQ-03）

### 3.2 页39 需求门户-查询进度

```
GET /api/v1/public/demands/{code}
  输入：queryCode（8 位）
  返回：PortalDemandTraceView （同 3.1 第三条）
  用途：游客凭查询码查脱敏进度（AC-REQ-04）
```

注意：补登/撤回在 Service（GuestDemandService.supplement/withdraw）但当前**无 HTTP 端点**，需前端会话决定是否要补 `/public/demands/{code}` 的 PUT/PATCH 端点，或降级为仅展示。

### 3.3 页48 AI 模型配置

```
POST /api/v1/ai-models
  请求：AiModelSaveReq 白名单 { modelName, baseUrl, apiKey, temperature, maxTokens, systemPrompt }
  返回：AiModelView （不含 api_key 明文/密文，仅脱敏掩码）
  权限：ipd:ai-document:add
```

约束：id / enabled 不可注入；update 时 apiKey=null 表示不改密钥；密钥不回显

### 3.4 页42 AI 文档助手

```
POST /api/v1/ai-documents/generate                 （PM 录入原始资料 → AI 输出 v1）
POST /api/v1/ai-documents/{id}/revise              （人工改版，HEAD 校验基准）
POST /api/v1/ai-documents/{id}/versions/{vid}/review （人工审核通过 → REVIEWED）
POST /api/v1/ai-documents/{id}/versions/{vid}/archive （归档，REQUIRED REVIEWED）
POST /api/v1/ai-documents/{id}/versions/{vid}/reject   （审核拒绝 → REJECTED，comment 必填）
GET  /api/v1/ai-documents/{id}/versions            （完整版本链 v1..vN）
GET  /api/v1/ai-documents/{id}/history             （版本链回溯视图）
GET  /api/v1/ai-documents/{id}/diff?from=&to=      （任意两版本字段级 diff）
```

业务串联：`GENERATED → REVIEWED → ARCHIVED`（+ `REJECTED` 兜底）

**风险提示（BR-AI-04，后端不审）**：前端在 AI 输出展示位置必须有「AI 生成、未经审核」角标 + 「确认无误请走审核流」提示。

---

## 四、行动项（建议给前端会话）

1. **建立前端仓工作项**：在本仓 `apps/web-antd/` 内的本地工作流恢复 4 张页卡为 todo（不要在后端仓 ruoyi-ai 复活 cancelled 卡）。
2. **逐页拉入端点契约**：按 §3 端点清单对接，禁止自造字段名（参考 `api/ipd/*.ts` 现有约定）。
3. **风险提示落位**：页42 AI 文档预览必须有「AI 生成、未经审核」角标（BR-AI-04），由前端 UI 校验。
4. **真 HTTP 业务闭环验证**：受后端 16039 不能重启约束，前端会话拉入后需在真联调环境跑端到端（不在本登记范围）。

---

## 五、后端测试证据（前端会话可作联调参考）

```
6 个验收测试类 79 用例全绿：
P412AcceptanceTest  14/14  补登/撤回+查询码追踪
P413AcceptanceTest   9/9   路由双 PM + 5 工作日提醒
P414AcceptanceTest  20/20  七态状态机 + ACCEPTED→CHANGED 变更单
P421AcceptanceTest  11/11  AI 模型配置
P422AcceptanceTest  15/15  AI 生成真业务闭环
P441AcceptanceTest  10/10  项目绩效汇总 + 三导出
合计：Tests run: 79, Failures: 0
```

兄弟会话代码已提交：
```
ee3b82ed fix(test,W28-3-OPS-09): P412 any(Object.class)→any(Requirement.class)
8ca0964a 同上 + log.md OPS-09 登记
6fc7086b test(ipd,P4-1.4): P414AcceptanceTest 20测全绿
547ba9f3 test(ipd,P4-1.4): P414AcceptanceTest 20测全绿
```

---

## 六、相关卡面状态（ruoyi-ai 项目看板）

| 卡 | task_id | 状态 |
|---|---|---|
| P4-5.1 P4 需求→AI 归档验收 | `0a293f3b-9a89-4171-895f-8685ddfde454` | done |
| P4-2 父级（AI 助手） | `98586804-d131-4056-8c41-6d7ac9f396d2` | done |
| P4-4 父级（报表导出） | `728b1113-f2ed-439e-bcbc-dd69df63ea8b` | done |
| P4-5 父级（验收回归） | `fe6b6ac7-7d27-4ce1-ba23-994db4935d08` | done |

4 份验收报告已 commit 落库（fc383ee7）：
```
docs/ipd-系统说明/验收/P4-1.x-单测复核记录-20260907.md
docs/ipd-系统说明/验收/P4-2.3-AI助手串联与风险提示-验收-20260907.md
docs/ipd-系统说明/验收/P4-4.1-单测复核与卡面推进-20260907.md
docs/ipd-系统说明/验收/P4-5.1-P4需求到AI归档验收-20260907.md
```

---

## 七、派单决策权

- 本会话仅做登记，不在本仓改前端代码
- 实施决定权归前端会话 / owner
- 本文档未 commit（按「未经明确要求不提交不推送」默认）；如需 commit，由 owner 决策