# IPD 19 个零引用权限码接入清单（A13 / 2026-09-07）

> **基线**: A10 报告发现 `apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.ts`
> 定义 66 个常量 key（65 distinct values），其中 19 个完全零引用 → 路由 meta.access 与
> 按钮 v-access:code 均未消费 → **隐式越权隐患（P0 安全风险）**。
>
> **任务**: A13 最小 diff 接入 19 个零引用权限码，仅加 `v-access:code` 属性或
> `meta.access` 数组追加，不动按钮文案/图标/事件/业务逻辑。

## 一、19 个接入码逐项清单

| # | 权限码 key (常量) | 字面值 | 接入位置 | 文件 | 行号 | 接入方式 |
|---|---|---|---|---|---|---|
| 1 | `GATE_ELEMENT_CREATE` | `ipd:gate-element:add` | "新增要素" 按钮 | `apps/web-antd/src/views/ipd/admin/gate-elements/index.vue` | L204 | `v-access:code` |
| 2 | `GATE_ELEMENT_UPDATE` | `ipd:gate-element:edit` | "编辑" 按钮 | `apps/web-antd/src/views/ipd/admin/gate-elements/index.vue` | L257 | `v-access:code` |
| 3 | `GATE_ELEMENT_DISABLE` | `ipd:gate-element:remove` | "停用" 按钮 | `apps/web-antd/src/views/ipd/admin/gate-elements/index.vue` | L262 | `v-access:code` |
| 4 | `CERT_TEMPLATE_CREATE` | `ipd:cert-template:add` | "新增认证项" 按钮 | `apps/web-antd/src/views/ipd/cert/templates/index.vue` | L249 | `v-access:code` |
| 5 | `CERT_TEMPLATE_DELETE` | `ipd:cert-template:remove` | "删除" 按钮 | `apps/web-antd/src/views/ipd/cert/templates/index.vue` | L305 | `v-access:code` |
| 6 | `DELETION_REQUEST_SUBMIT` | `ipd:deletion-request:submit` | "发起删除申请" 按钮 | `apps/web-antd/src/views/ipd/deletion/my-requests/index.vue` | L128 | `v-access:code` |
| 7 | `DELETION_REQUEST_LEADER` | `ipd:deletion-request:leader` | "提交审核意见" 按钮（组长权限分支） | `apps/web-antd/src/views/ipd/deletion/review/index.vue` | L147 | `v-access:code` 数组 `[LEADER, ADMIN]` |
| 8 | `DELETION_REQUEST_ADMIN` | `ipd:deletion-request:admin` | "升级超期申请" 按钮 + "提交审核意见"（超管分支） | `apps/web-antd/src/views/ipd/deletion/review/index.vue` | L147, L168 | `v-access:code` |
| 9 | `AI_DOCUMENT_CREATE` | `ipd:ai-document:add` | "开始生成" + "登记 AI 输出" 按钮 | `apps/web-antd/src/views/ipd/ai-docs/index.vue` | L503, L539 | `v-access:code` |
| 10 | `AI_DOCUMENT_REVIEW` | `ipd:ai-document:review` | "审核通过" + "审核拒绝" 按钮 | `apps/web-antd/src/views/ipd/ai-docs/index.vue` | L604, L612 | `v-access:code` |
| 11 | `AI_DOCUMENT_REVISE` | `ipd:ai-document:edit` | "归档" 按钮（审核通过后） | `apps/web-antd/src/views/ipd/ai-docs/index.vue` | L625 | `v-access:code` |
| 12 | `SOP_TEMPLATE_LIST` | `ipd:sop-template:list` | "查看" 按钮 | `apps/web-antd/src/views/ipd/admin/sop-template/index.vue` | L335 | `v-access:code` |
| 13 | `AI_MODEL_LIST` | `ipd:ai-model:list` | AI 模型配置页路由 meta.access | `apps/web-antd/src/router/routes/modules/ipd.ts` | L438 | `meta.access` 数组追加 |
| 14 | `PRODUCT_CREATE` | `ipd:product:add` | "新增产品" 按钮 | `apps/web-antd/src/views/ipd/product/list/index.vue` | L267 | `v-access:code` |
| 15 | `GATE_REVIEW_LIST` | `ipd:gate-review:list` | "加载评审视图" 按钮 | `apps/web-antd/src/views/ipd/review/gate-panel.vue` | L303 | `v-access:code` |
| 16 | `GATE_REVIEW_APPROVE` | `ipd:gate-review:edit` | "驳回" + "通过" 双签按钮 | `apps/web-antd/src/views/ipd/review/gate-panel.vue` | L367, L368 | `v-access:code` |
| 17 | `GATE_REVIEW_INITIATE` | `ipd:gate-review:add` | "提交阶段确认" 按钮 | `apps/web-antd/src/views/ipd/review/index.vue` | L139 | `v-access:code` |
| 18 | `STAGE_ACTION_EXECUTE` | `ipd:stage-action:edit` | "保存字段" 按钮 | `apps/web-antd/src/views/ipd/project/action-detail/index.vue` | L468 | `v-access:code` |
| 19 | `STAGE_ACTION_DELIVERABLE` | `ipd:stage-action:add` | "登记交付物" 按钮 | `apps/web-antd/src/views/ipd/project/action-detail/index.vue` | L471 | `v-access:code` |

## 二、修改文件清单（8 个 view + 1 个路由）

### view 文件（8 个）
1. `apps/web-antd/src/views/ipd/admin/gate-elements/index.vue`（3 按钮）
2. `apps/web-antd/src/views/ipd/cert/templates/index.vue`（2 按钮）
3. `apps/web-antd/src/views/ipd/deletion/my-requests/index.vue`（1 按钮）
4. `apps/web-antd/src/views/ipd/deletion/review/index.vue`（2 按钮）
5. `apps/web-antd/src/views/ipd/ai-docs/index.vue`（5 按钮）
6. `apps/web-antd/src/views/ipd/admin/sop-template/index.vue`（1 按钮）
7. `apps/web-antd/src/views/ipd/product/list/index.vue`（1 按钮）
8. `apps/web-antd/src/views/ipd/review/index.vue` + `apps/web-antd/src/views/ipd/review/gate-panel.vue`（3 按钮跨 2 文件）
9. `apps/web-antd/src/views/ipd/project/action-detail/index.vue`（2 按钮）

### 路由文件（1 个）
- `apps/web-antd/src/router/routes/modules/ipd.ts`（1 处 meta.access 追加）

### 测试文件（1 个）
- `apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.test.ts`（+19 测试用例 + 1 数量断言）

## 三、最小 diff 原则遵守

- 仅在 `<Button>` / `<button>` / `<Card>` 上追加 `v-access:code="IPD_PERMISSION_CODES.XXX"`
- 仅在 router `meta.access` 数组中追加元素
- 未触动：按钮文案、图标、loading 状态、disabled 逻辑、@click 处理器、ref/reactive 定义
- 未触动：`_shared/ipd-permission-codes.ts` 常量定义本身
- 未触动：`useAccess()` 工具函数与 `v-access` 指令实现

## 四、测试策略

### 新增测试（`_shared/ipd-permission-codes.test.ts`）
1. **数量断言**：19 个目标码与 A13 承诺一致
2. **契约扫描**：对每个码做 fs 静态扫描 `views/ipd` 与 `router`，验证其字面值在源文件中实际出现
   - 防止后续重构无意中删除 `v-access:code` 或 `meta.access` 元素（回归保护）
   - 故意不依赖运行时 `useAccess` mock，避免依赖 vben access 包结构

### 测试结果
- `pnpm exec vitest run --config vitest.ipd.config.mts`：717 → **737 passed**（+20 = 19 码测试 + 1 数量断言）
- 1 skipped（auth-live 长期 mock，与本任务无关）
- 0 失败

## 五、验收命令结果

| 命令 | 结果 |
|---|---|
| `pnpm --filter @vben/web-antd run typecheck` | exit 0（无错误） |
| `pnpm exec vitest run --config vitest.ipd.config.mts` | 737 passed, 1 skipped, 0 failed（exit 0） |
| `rg "IPD_PERMISSION_CODES" apps/web-antd/src/views/ipd -c` | 14 个 view/路由文件引用（详见报告） |

## 六、剩余 0 引用权限码（待 owner 复核）

接入 19 后，仍有 **15 个码完全零引用**（A13 未在本轮处理，建议 owner 复核）：

### 优先级 1：与现有视图弱关联
- `PROJECT_QUERY` (`ipd:project:query`) — 项目查询，前端走 keyword 搜索（可能后端预留）
- `PROJECT_STATUS_CHANGE` (`ipd:project:edit`) — 项目状态变更，目前只读 Tag，无切换按钮
- `PRODUCT_BIND_PROJECT` (`ipd:product:edit`) — 产品绑定项目，product/workspace 切换按钮非绑定语义
- `GATE_ELEMENT_PUBLISH` (`ipd:gate-element:publish`) — 评审要素发布（当前 admin 页无此按钮）
- `GATE_ELEMENT_ARCHIVE` (`ipd:gate-element:archive`) — 评审要素归档（同上）
- `GATE_ELEMENT_COPY` (`ipd:gate-element:copy`) — 评审要素复制（同上）
- `GATE_ELEMENT_REVERT` (`ipd:gate-element:revert`) — 评审要素历史恢复（同上）

### 优先级 2：后端未交付相关 UI
- `NOTIFICATION_READ` (`ipd:notification:read`) — 通知已读，workbench 显示数量但无显式按钮
- `NOTIFICATION_DISPATCH` (`ipd:notification:dispatch`) — 通知分发（管理端），无对应视图
- `COEFFICIENT_PROPOSE` (`ipd:coefficient:propose`) — 系数提议，kpi 模块未对应
- `COEFFICIENT_CONFIRM` (`ipd:coefficient:confirm`) — 系数确认，同上
- `COMPLIANCE_READ` (`ipd:compliance:read`) / `COMPLIANCE_WRITE` (`ipd:compliance:write`) — 合规读/写，无合规视图
- `SWITCHING_ACCEPTANCE_QUERY` (`ipd:switching-acceptance:query`) / `SWITCHING_ACCEPTANCE_ADMIN` (`ipd:switching-acceptance:admin`) — 切换验收，无对应视图
- `STAGE_ACTION_LIST` (`ipd:stage-action:list`) / `STAGE_ACTION_INSTANTIATE` (`ipd:stage-action:add`) — 阶段动作列表/实例化（注意 INSTANTIATE 与 DELIVERABLE 同码，DELIVERABLE 已接入）

### 优先级 3：定义可疑（建议 owner 复核）
- `STAGE_ACTION_DELIVERABLE` 与 `STAGE_ACTION_INSTANTIATE` **共享同一字面值** `ipd:stage-action:add`
  （_shared 测试已记录此设计；后端若期望两码独立，建议复核）

## 七、未提交/未推送

按任务要求，仅做本地修改，未执行 git commit/push。修改文件清单：

```
modified:   apps/web-antd/src/router/routes/modules/ipd.ts
modified:   apps/web-antd/src/views/ipd/admin/gate-elements/index.vue
modified:   apps/web-antd/src/views/ipd/admin/sop-template/index.vue
modified:   apps/web-antd/src/views/ipd/ai-docs/index.vue
modified:   apps/web-antd/src/views/ipd/cert/templates/index.vue
modified:   apps/web-antd/src/views/ipd/deletion/my-requests/index.vue
modified:   apps/web-antd/src/views/ipd/deletion/review/index.vue
modified:   apps/web-antd/src/views/ipd/product/list/index.vue
modified:   apps/web-antd/src/views/ipd/project/action-detail/index.vue
modified:   apps/web-antd/src/views/ipd/review/gate-panel.vue
modified:   apps/web-antd/src/views/ipd/review/index.vue
modified:   apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.test.ts
```

## 八、A23 剩余 17 个零引用权限码处置报告（Wave 5 / 2026-09-07）

> **性质**：A13 接入 19 后，仍余 §六列出的 17 个零引用码（按 §六 优先级 1=7 + 优先级 2=10，含 STAGE_ACTION_INSTANTIATE 字面值碰撞案例）。
> **处置策略**：经全量 grep `apps/web-antd/src/views/ipd` 与 `apps/web-antd/src/router` 三轮验证，17 个码在视图/路由源文件中均**无对应按钮、路由或 meta.access**，故统一采用 **Disposition 1：`/* reserved */` 行尾注释**（仅 STAGE_ACTION_INSTANTIATE 改用碰撞 doc 注释）。
> **理由**：A13 已穷尽存在按钮的接入；A23 阶段前端无对应 UI，后端能力存在但待前端页面交付（owner 决议点：先注释标记，待 UI 实装后再迁移至 `v-access:code` / `meta.access`）。

### 8.1 17 个码逐项处置

| # | 常量 key | 字面值 | 处置 | 文件 : 行号 | 原因 |
|---|---|---|---|---|---|
| 1 | `PROJECT_QUERY` | `ipd:project:query` | reserved (A23) | `ipd-permission-codes.ts:20` | 后端预留查询权限，前端走 keyword 搜索无独立按钮 |
| 2 | `PROJECT_STATUS_CHANGE` | `ipd:project:edit` | reserved (A23) | `ipd-permission-codes.ts:22` | 项目状态切换按钮未实装（项目详情只读 Tag） |
| 3 | `PRODUCT_BIND_PROJECT` | `ipd:product:edit` | reserved (A23) | `ipd-permission-codes.ts:28` | 产品绑定项目语义与 workspace 切换按钮不一致 |
| 4 | `GATE_ELEMENT_PUBLISH` | `ipd:gate-element:publish` | reserved (A23) | `ipd-permission-codes.ts:46` | 评审要素发布按钮未实装（admin 页只有 CREATE/UPDATE/DISABLE） |
| 5 | `GATE_ELEMENT_ARCHIVE` | `ipd:gate-element:archive` | reserved (A23) | `ipd-permission-codes.ts:47` | 评审要素归档按钮未实装 |
| 6 | `GATE_ELEMENT_COPY` | `ipd:gate-element:copy` | reserved (A23) | `ipd-permission-codes.ts:48` | 评审要素复制按钮未实装 |
| 7 | `GATE_ELEMENT_REVERT` | `ipd:gate-element:revert` | reserved (A23) | `ipd-permission-codes.ts:49` | 评审要素历史恢复按钮未实装 |
| 8 | `NOTIFICATION_READ` | `ipd:notification:read` | reserved (A23) | `ipd-permission-codes.ts:68` | workbench 仅显示未读数量，无显式"已读"按钮 |
| 9 | `NOTIFICATION_DISPATCH` | `ipd:notification:dispatch` | reserved (A23) | `ipd-permission-codes.ts:69` | 通知管理端无对应视图 |
| 10 | `COEFFICIENT_PROPOSE` | `ipd:coefficient:propose` | reserved (A23) | `ipd-permission-codes.ts:64` | kpi 模块未对应系数提议 UI |
| 11 | `COEFFICIENT_CONFIRM` | `ipd:coefficient:confirm` | reserved (A23) | `ipd-permission-codes.ts:65` | kpi 模块未对应系数确认 UI |
| 12 | `COMPLIANCE_READ` | `ipd:compliance:read` | reserved (A23) | `ipd-permission-codes.ts:114` | 无合规视图 |
| 13 | `COMPLIANCE_WRITE` | `ipd:compliance:write` | reserved (A23) | `ipd-permission-codes.ts:115` | 无合规视图 |
| 14 | `SWITCHING_ACCEPTANCE_QUERY` | `ipd:switching-acceptance:query` | reserved (A23) | `ipd-permission-codes.ts:105` | 切换验收查询无对应视图 |
| 15 | `SWITCHING_ACCEPTANCE_ADMIN` | `ipd:switching-acceptance:admin` | reserved (A23) | `ipd-permission-codes.ts:106` | 切换验收管理端无对应视图 |
| 16 | `STAGE_ACTION_LIST` | `ipd:stage-action:list` | reserved (A23) | `ipd-permission-codes.ts:31` | 阶段动作列表无独立入口，详情页内联渲染 |
| 17 | `STAGE_ACTION_INSTANTIATE` | `ipd:stage-action:add` | reserved (A23) **+ 碰撞 doc** | `ipd-permission-codes.ts:34` | 与 `STAGE_ACTION_DELIVERABLE` 同字面值（已 A13 接入 action-detail/index.vue:471 "登记交付物"）；本次仅加碰撞 doc 注释，**未重复加 `v-access:code`**（owner 决议点：是否拆分为两个独立码） |

### 8.2 修改文件清单（A23）

```
modified:   apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.ts        (17 行尾注释，无常量定义改动)
modified:   apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.test.ts    (+19 测试：1 计数 + 1 distinct + 17 per-code)
```

**未触动**：`_shared/ipd-permission-codes.ts` 常量定义本身、`useAccess()` / `v-access` 框架实现、router `ipd.ts`（本轮无 `meta.access` 追加，因所有 17 码都无对应路由页面承载能力）。

### 8.3 验收命令结果

| 命令 | 结果 |
|---|---|
| `pnpm --filter @vben/web-antd run typecheck` | exit 0，0 errors |
| `pnpm exec vitest run --config vitest.ipd.config.mts` | **796 passed, 1 skipped, 0 failed** |
| `ipd-permission-codes.test.ts` 单文件 | 49 tests（30 旧 + 19 新），全部通过 |
| `grep -c "reserved (A23)" ipd-permission-codes.ts` | 17（与 §六清单一一对齐） |

### 8.4 决策点说明（owner 复核建议）

1. **STAGE_ACTION_INSTANTIATE 碰撞拆分**：当前与 `STAGE_ACTION_DELIVERABLE` 共享 `ipd:stage-action:add`，A13 已为 DELIVERABLE 在 `action-detail/index.vue:471` "登记交付物" 按钮加 `v-access:code`。如后端语义上确实是两个权限（动作实例化 ≠ 交付物登记），建议后端拆分为独立字符串后，前端再分别为 INSTANTIATE 加按钮钩子。本次仅注释标记碰撞事实。

2. **17 个 reserved 码的迁移路径**：当后续某个前端页面交付对应能力时，按"反向迁移"流程：
   - 在新视图/路由加 `v-access:code` 或 `meta.access`
   - 移除 `_shared/ipd-permission-codes.ts` 该行的 `/* reserved (A23) */` 注释
   - 删除 A23 测试套件对应 `it()`（注释测试不再适用）

3. **未走 `meta.access` 路由追加**：考虑过将 PROJECT_QUERY 加到 `/ipd/projects` 路由、`PRODUCT_BIND_PROJECT` 加到 `/ipd/products` 路由等。**否决理由**：路由级授权意味着"无权限用户连列表都看不到"，但前端 keyword 搜索/产品 workspace 切换等已全员开放，不应因后端预留码而突然限权。等待前端 UI 实装 + 业务确认限权范围后再走路由级授权。

### 8.5 未提交/未推送

按 AGENTS.md 红线，仅做本地修改，未执行 git commit/push / 分支 / PR。

