# IPD-Web 前端代码健康度报告

**扫描日期**：2026-09-07
**扫描范围**：`apps/web-antd/src/**.{ts,vue}` (575 个文件：305 vue + 270 ts)
**扫描器**：rg + 手工复核（repowise `get_dead_code` 当前索引未覆盖 web-antd 全量，仅做兜底交叉验证）
**说明**：本报告基于 IPD 治理三轮（A8 拼写/A9 视觉一致性/A10 健康度）后代码状态扫描，涵盖死代码、重复模式、SSOT 偏离、类型漂移四大维度。

---

## 一、健康度评分

| 维度 | 评分 (0-100) | 说明 |
|---|---|---|
| **SSOT 收敛** | **85** | 错误码/格式化/状态机/权限码已建 4 张 SSOT 表，治理良好 |
| **死代码清理** | **72** | 11 个 deprecated shim 仍被引用、_shared 中 1 文件零生产消费、ipd-enums 18 个导出零外部消费 |
| **重复模式治理** | **60** | 12 处 `STATUS_TEXT/COLOR` 重复、16 个 normalize 函数、20 处内联空值判断 |
| **类型健康** | **55** | 642 处 `: any` 使用，44% 集中在 workflow-designer 三方包 |
| **路由/页面结构** | **78** | IPD 路由 47 条全部消费，但 detail/incentive.vue 是占位 |
| **综合分** | **70** | 中等偏上；前端 SSOT 体系已成型，下一步是「重复模式归并 + 类型收紧」 |

---

## 二、死代码清单

### 2.1 `_shared/` 导出但零生产的标识符（最高优先级）

| 文件 | 状态 |
|---|---|
| `_shared/ipd-breakpoints.ts` | **零生产消费**（仅 test 自身） — 需复核是否被 css 或 SCSS 间接引用 |
| `_shared/ipd-enums.ts` 18 个导出 | 仅 `bonus-pool/index.vue` 消费 3 个 (`bonusStateLabel/Tone/STATUS_TONE`)；`ROLE_TEXT/PRIORITY_TEXT/SEVERITY_TEXT/roleText/priorityText/priorityTone/severityText/severityTone/projectStateLabel/projectStateTone/demandStateLabel/demandStateTone/bonusStateTone/deletionStateLabel/deletionStateTone/gateStateLabel/gateStateTone/changeStateLabel/changeStateTone/STAGE_TONE` 均为外部零消费 |

### 2.2 Deprecated shim 仍被引用（治理缺口）

| Shim 文件 | 仍消费的文件数 | 治理建议 |
|---|---|---|
| `views/ipd/bid/bid-error.ts` | 6 个文件 (bid/create, bid/select, bid/respond, bid/list, bid/bid-error.test, change/index.vue) | 改用 `_shared/ipd-error-text.ts` 的 `ipdErrorText(error, { domain: 'bid' })`，迁移完删除 shim |
| `views/ipd/project/project-error.ts` | 8 个文件 (project/create, project/list, project/legacy-import, project/detail/flow, project/detail/overview, project/action-detail, project/change-detail, review/index.vue, change/index.vue) | 同上，`domain: 'project'` |

### 2.3 IPD 路由已注册但几乎无直接引用的 `name`

全部 47 条 IPD 路由**均有 component 消费**（动态 import `() => import('#/views/...')`），无未引用路由。仅以下属于「兼容占位」非死代码：
- `IpdAdminAuditLogs` (path `audit-logs-old`) — 仅用于兼容旧跳转，注释明确「保留避免外部引用断链」
- `IpdProducts/IpdProductManage/IpdProductCreate/IpdProductEdit` — 4 条产品子路由都挂了真组件，但 `IpdProducts` 顶层没有 component，会落到 `_core/fallback/not-found.vue`（注释明确：菜单入口已切到工作台「产品管理」→ IpdProductManage）

### 2.4 IPD 子页签占位组件

| 文件 | 行数 | 性质 |
|---|---|---|
| `views/ipd/project/detail/incentive.vue` | 35 | 实质挂 `BackendPending` 占位，业务真功能走 `/ipd/incentive/bonus-pool`，路由 `meta.ipdCard: 'P0-10.37'` 已登记项目维聚合端点缺失 |

### 2.5 顶层 any/未消费 import 风险点（按 hotness 排序）

> 注：`: any` 642 处多为 `*.d.ts` 自动生成或 `packages/` 三方基础设施，本表仅列**应用代码**热点。

| 文件 | any 次数 | 风险 |
|---|---|---|
| `apps/web-antd/src/packages/workflow-designer/StandaloneWorkflowDesigner.vue` | 17 | 第三方工作流设计器，前端未深度维护，**标记规避** |
| `apps/web-antd/src/packages/workflow-designer/components/RunDetail.vue` | 17 | 同上 |
| `apps/web-antd/src/packages/workflow-designer/utils/workflow-util.ts` | 6 (as any) | 同上 |
| `apps/web-antd/src/views/knowledge/info/detail/components/FileManagement.vue` | 10 | 应用代码，建议收敛 |
| `apps/web-antd/src/views/workflow/processDefinition/index.vue` | 7 | 应用代码 |
| `apps/web-antd/src/utils/file/download.ts` | 8 (含 as any) | 工具函数，建议固化类型 |

---

## 三、重复模式清单

### 3.1 `STATUS_TEXT` / `STATUS_COLOR` 状态映射表（应有 SSOT）

> 顶层 `_shared/ipd-state-machines.ts` + `_shared/ipd-enums.ts` 已是 SSOT，但消费侧仍在重复定义：

| 文件 | 表名 | 建议迁入 |
|---|---|---|
| `views/ipd/project/project-display.ts` | `PROJECT_STATUS_TEXT/COLOR、STAGE_TEXT/COLOR、LEVEL_TEXT、TEMPLATE_TEXT、SOURCE_TEXT、CATCHUP_TEXT、DEPTH_TEXT/COLOR、ACTION_STATUS_TEXT/COLOR、PERSON_TYPE_TEXT` | 9 张表全可迁入 `ipd-state-machines.ts`（PROJECT/STAGE 已建），其余进 `ipd-enums.ts` |
| `views/ipd/bid/bid-display.ts` | `BID_STATUS_TEXT/COLOR、BID_MODE_TEXT、RESPONSE_STATUS_TEXT/COLOR` | 迁入 `ipd-state-machines` |
| `views/ipd/demand/index.vue` | `DEMAND_STATUS_TEXT` | `ipd-state-machines.DEMAND_STATUS_MACHINE.states` 已含 label |
| `views/ipd/incentive/bonus-pool/index.vue` | `BONUS_STATUS_TEXT` | 同上 |
| `views/ipd/portal/status/index.vue` | `STATUS_TEXT` | 应迁入 `ipd-enums.STATUS_TONE` 同伴的中文表 |
| `views/ipd/change/index.vue` | `STATUS_TEXT`（Object.fromEntries 派生） | 派生尚可接受 |
| `views/ipd/workbench/index.vue` | `STATUS_TEXT` | 同 |
| `views/ipd/project/detail/changes.vue` | `STATUS_TEXTS` | 应迁 |
| `views/ipd/product/workspace/index.vue` | `PRODUCT_STATUS_TEXT` | 应迁 |
| `api/ipd/deletion.ts` | `DELETION_STATUS_TEXT` | 在 api 层但消费端在 view，已统一 |

**结论**：12 处状态中文表，6 处可被 `ipd-state-machines.states[i].label` 直接替换，4 处需新增 SSOT 表项。

### 3.2 normalize 函数（应用层 API 层重复样板）

| 文件 | 函数 | 数量 |
|---|---|---|
| `api/ipd/gate-element.ts` | `normalize(raw)` | 1 |
| `api/ipd/project.ts` | `normalizeProject(raw)` | 1 |
| `api/ipd/ai-document.ts` | `normalizeDateTime(value)` | 1 |
| `api/ipd/cert-template.ts` | `normalizeCert(raw)` | 1 |
| `api/ipd/system-config.ts` | `normalize(raw)`, `normalizeVersion(raw)` | 2 |
| `api/ipd/ai-model-config.ts` | `normalize(raw)` | 1 |
| `api/ipd/product.ts` | `normalizeProduct(raw)`, `normalizeGroup(raw)`, `normalizeList(data, normalize)` | 3 |
| `api/ipd/stage-action.ts` | `normalizeAction(raw)`, `normalizeActionList(data)` | 2 |
| `api/ipd/sop-template.ts` | `normalizeItem(raw)` | 1 |
| `api/request.ts` | `normalizeResponseDateTimes(value)` | 1 |
| `packages/@core/base/shared/src/utils/date.ts` | `normalizeDateInput(time)` | 1 |
| `packages/utils/src/helpers/generate-routes-backend.ts` | `normalizeViewPath(path)` | 1 |

**建议**：抽 `createNormalizer<T>(defaultTransforms)` 模板于 `_shared/api-normalize.ts`，消除 8 处重复。

### 3.3 空值兜底表达式

> `if (value === null || value === undefined || value === '')` 共 **20+ 处**重复，应统一调 `format.ts.isBlank(value)`（**已有但仅 5 处调用！**）。建议：
- 把 `isBlank` export（当前未导出）；
- 替换 15 处内联空值判断。

### 3.4 `Record<string, string>` 状态字典

> 119 处 `Record<string, string>` 定义 — 部分可改用 enum 字面量联合类型 + `as const`，获 TS 收窄收益。

### 3.5 `STAGE_TEXT/COLOR` 与 `project-display.ts` STAGE_TEXT

`project-display.ts` 第 41-69 行独立维护 6 阶段 label + tone，与 `ipd-enums.STAGE_TONE`（label 在 `ipd-state-machines.PROJECT_STATES[i].label`）**数据不一致风险**（前者用 'default/processing/success'、后者亦同，但阶段顺序与显示策略各自演进）。

---

## 四、未用 import / 未用 export 清单（top 20）

### 4.1 `_shared/` 模块消费热力图

| 模块 | 外部消费文件数 | 状态 |
|---|---|---|
| `_shared/ipd-error-text.ts` | ≥8 文件 | 健康（SSOT 已生效） |
| `_shared/zk-ipd-rules.ts` | 8 文件 | 健康 |
| `_shared/use-filter-sync.ts` | 1 文件（project/list） | **消费不足**，可能命名太抽象 |
| `_shared/use-focus-trap.ts` | 1 文件（layouts/ipd.vue） | 健康（单点消费符合预期） |
| `_shared/format.ts` | 27 文件 | 健康 |
| `_shared/ipd-state-machines.ts` | 3 文件（enums/change/change-detail） | 健康 |
| `_shared/ipd-permission-codes.ts` | 1 文件（ipd.ts 路由） | 健康（SSOT 唯一消费点） |
| `_shared/ipd-enums.ts` | 1 文件（bonus-pool） | **消费严重不足** — 18 个导出仅 3 个被消费 |
| `_shared/ipd-breakpoints.ts` | 0 文件（仅 test） | **疑似死代码** |

### 4.2 权限码常量未消费（IPD_PERMISSION_CODES 中 53 码，已用 34，未用 19）

**未消费的 19 个码**（均为声明为 SSOT，可能为「备而不用」或「前端未接入」）：

```
PROJECT_STATUS_CHANGE / PRODUCT_CREATE / PRODUCT_BIND_PROJECT
STAGE_ACTION_LIST / STAGE_ACTION_EXECUTE
CERT_TEMPLATE_CREATE / CERT_TEMPLATE_DELETE
GATE_ELEMENT_CREATE / UPDATE / DISABLE / PUBLISH / ARCHIVE / COPY / REVERT
DELETION_REQUEST_ARCHIVE / SUBMIT / LEADER / ADMIN
GATE_REVIEW_LIST / INITIATE / APPROVE
COEFFICIENT_PROPOSE / CONFIRM
NOTIFICATION_READ / DISPATCH
AI_MODEL_LIST / SOP_TEMPLATE_LIST
COMPLIANCE_READ / WRITE
SWITCHING_ACCEPTANCE_QUERY / ADMIN
```

**判断**：这些大概率是「后端已交付但前端按钮 v-access:code 漏挂」，不是死码。**建议：开第四轮专项——权限码接入审计**。

### 4.3 TOP 未消费 import 风险

> 因 TypeScript `noUnusedLocals`/`noUnusedParameters` 默认未开或被忽略，需 lint 兜底；建议在 `eslint.config.mjs` 的 ipd 块加 `no-unused-vars: ['error', { argsIgnorePattern: '^_' }]`。

---

## 五、类型漂移 `any` 使用热点

| 区域 | any 处数 | 备注 |
|---|---|---|
| `packages/workflow-designer/**`（第三方移植） | ~60+ | 隔离区，建议原样保留 |
| `apps/web-antd/src/api/**.d.ts`（自动生成） | ~70 | 自动生成，保留 |
| `packages/@core/**/types.ts` | ~30 | 框架基础设施 |
| **应用代码（IPD views）** | <50 | 主要在 trace-detail-drawer.vue、knowledge/file-management、workflow/processDefinition |
| `apps/web-antd/src/views/workflow/**` | ~25 | 流程模块通用，工作量中等 |

**重点修复建议**：
1. `views/knowledge/info/detail/components/FileManagement.vue` (10 处)
2. `views/monitor/trace/trace-detail-drawer.vue` (4 处)
3. `views/workflow/processDefinition/index.vue` (7 处)
4. `utils/file/download.ts` (4 处) — 固化 `BlobRequestConfig<T>` 泛型

---

## 六、建议抽取位置

| 重复 | 抽取位置 | 工作量 |
|---|---|---|
| 12 处 STATUS_TEXT | `_shared/ipd-state-machines.ts` (新增 label 字段) + `_shared/ipd-enums.ts` (新增表项) | 2 PD |
| 8 处 normalize | `_shared/api-normalize.ts` | 1 PD |
| 20 处空值判断 | `_shared/format.ts` export `isBlank` | 0.5 PD |
| ipd-enums 18 个未消费 export | 业务页面接入 + 注释哪些为备用 | 1 PD |
| 19 个权限码 | 业务按钮挂 `v-access:code` | 1.5 PD |
| 11 个 deprecated shim 迁移 | 替换 import → 删 shim | 1 PD |

**合计**：约 7 PD（1 人 1.5 周）

---

## 七、是否需要派单第四轮专项治理？

### 答案：**需要，建议分两单并发**。

| 派单 | 名称 | 范围 | 工作量 |
|---|---|---|---|
| **A11** | IPD 状态/枚举 SSOT 收敛 | 把 12 处 STATUS_TEXT 迁入 `_shared/ipd-state-machines.ts` + `_shared/ipd-enums.ts`；删除 `project-display.ts/bid-display.ts` 的重复表（保留时间格式化函数）；ipd-enums 18 个未消费导出要么接入要么删除 | 3 PD |
| **A12** | IPD 错误 shim 退役 + 权限码接入 | 11 个 deprecated shim 全部替换 import 指向 `_shared/ipd-error-text.ts` 后删除；19 个未消费 `IPD_PERMISSION_CODES.*` 接入业务按钮或标记为 `/* reserved */` | 3 PD |

**两单互不依赖**，可同周并行。A11 完成后健康度综合分预估提升到 **78**，A12 完成后到 **82**。

---

## 八、附录：扫描方法

```bash
# 死代码
rg "_shared/(format|ipd-enums|ipd-state-machines|use-filter-sync|use-focus-trap|ipd-breakpoints|ipd-permission-codes|zk-ipd-rules|ipd-error-text)['\"]" --type ts --type vue

# 重复模式
rg "STATUS_TEXT.*Record<|COLOR.*Record<" --type ts --type vue
rg "function normalize" --type ts
rg "if \(value === null \|\| value === undefined \|\| value === ''\)" --type ts --type vue

# 类型漂移
rg ": any" --type ts --type vue
rg " as any" --type ts --type vue -c

# 路由
rg "name: 'Ipd[A-Z]" /apps/web-antd/src/router/routes/modules/ipd.ts
rg "IPD_PERMISSION_CODES\.[A-Z_]+" --type ts --type vue -o
```

**免责**：本扫描基于 `rg` 文本层，TypeScript 类型层未跑（避免改动 `tsconfig`/`.d.ts` 触发 baseLine 漂移）；任何 `unused` 判定请人工最终确认 — 部分可能为 d.ts 自动生成或被动态 `import()` 引用。
