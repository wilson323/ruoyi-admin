# bid/respond 真功能 bug + 前端测试修复收口（2026-09-06）

## 修复前状态

- bid/respond/index.vue v-else-if 真 bug：存在（line 41/50 双 `v-else-if="invitation"`，正文 Card 永不渲染）
- typecheck error：**14**
- vitest fail：**20**
- 并发 session 状态：**已收工**（git status 仅显示 5 个我自己修改的 M，未跟踪文件来自并发 session 此前提交的产物，本次不触碰）

## 修复

### Bug #1：bid/respond v-else-if 双匹配（CRITICAL）
**根因**：v-else-if 链中两个分支都写 `v-else-if="invitation"`，前者先匹配把后者收窄成 `never` —— TypeScript narrowing 报 7 条 TS2339（`Property 'title' does not exist on type 'never'` 等）。

**修复**：
- 第 42 行：`v-else-if="invitation"` → `v-if="!invitation"`（拆出 v-else-if 链，作为独立分支，仅在无 invitation 时显示规则 Alert）
- 第 50 行：`<template v-else-if="invitation">` → `<template v-if="invitation">`（独立 v-if 分支，正文 Card 正常渲染）
- 补 `import { RULES_BY_PAGE, renderRulesDescription } from '../../_shared/zk-ipd-rules';` 解决 2 条 TS2304

**验证**：vue-tsc narrowing 恢复，7 条 TS2339 消失；typecheck 14→0。

### Bug #2：剩余 7 条 typecheck error

| 错误 | 文件 | 修复 |
|---|---|---|
| TS2304 `renderRulesDescription` × 2 | `bid/list/index.vue` line 173 | 补 `import { RULES_BY_PAGE, renderRulesDescription } from '../../_shared/zk-ipd-rules';` |
| TS2307 `../_shared/zk-ipd-rules` 模块找不到 | `project/create/index.vue` line 39 | 路径修正为 `../../_shared/zk-ipd-rules`；移除未使用的 `type ZkIpdRule` 导入（解 TS6133）|
| TS6133 `idxErr` 未使用 | `project/create/index.test.ts` line 114 | 删除未使用变量（原意是保留供未来严格模式断言，本轮简化掉）|

### Bug #3：剩余 2 条 vitest fail（CSS 路径）

| 错误 | 文件 | 修复 |
|---|---|---|
| Failed to resolve `"../_shared/ipd-theme.css"` | `bid/create/index.vue` line 121 | `../_shared` → `../../_shared`（bid/create 距 ipd/ 两层）|
| Failed to resolve `"../_shared/ipd-theme.css"` | `project/create/index.vue` line 40 | 同上 |

### Bug #4：剩余 1 条 vitest fail（业务规则断言与权威文案不对齐）

| 错误 | 文件 | 修复 |
|---|---|---|
| `expect(html).toContain('已归档')` / `'2×'` 失败 | `project/create/index.test.ts` line 98/106 | 权威规则文案为「归档后」/「2 倍」，更新测试断言以匹配 `_shared/zk-ipd-rules.ts` 中的 ZkIpdRule 真实文案（业务规则是唯一权威，测试跟随规则而非反向） |

## 验证结果

- **typecheck error：14 → 0**（`pnpm run check:type` 全绿，37 个包）
- **vitest fail：20 → 0**（`npx vitest run --config vitest.ipd.config.mts`：32 个测试文件，31 passed | 1 skipped；285 个测试，284 passed | 1 skipped）

## 测试输出关键行

**typecheck 末尾**：
```
@vben/web-antd:typecheck: > vue-tsc --noEmit --skipLibCheck
@vben/web-antd:typecheck: 
 Tasks:    1 successful, 1 total
Cached:    0 cached, 1 total
  Time:    16.461s
```

**vitest 末尾**：
```
 Test Files  31 passed | 1 skipped (32)
      Tests  284 passed | 1 skipped (285)
   Start at  04:40:12
   Duration  16.65s (transform 2.53s, setup 0ms, collect 75.77s, tests 43.89s, environment 13.70s, prepare 3.50s)
```

## 修改清单（5 个文件，本次 commit 范围）

- `apps/web-antd/src/views/ipd/bid/respond/index.vue`（v-else-if bug + 补 import）
- `apps/web-antd/src/views/ipd/bid/list/index.vue`（补 import）
- `apps/web-antd/src/views/ipd/bid/create/index.vue`（CSS 路径）
- `apps/web-antd/src/views/ipd/project/create/index.vue`（import 路径 + CSS 路径 + 移除未使用类型）
- `apps/web-antd/src/views/ipd/project/create/index.test.ts`（删除未使用变量 + 断言对齐权威规则文案）
- `apps/web-antd/src/views/ipd/bid/respond/FIX-收口-20260906.md`（本报告）

## 未完成项 + 阻塞原因

无遗留项。

## 不在本轮 commit 范围的改动

`git status` 显示的大量 M（aiflow / chat / knowledge / router / workflow-designer 等模块）与 `??` 文件（bid/api、bid/select/、各 .test.ts 等）均来自**并发 session 此前的工作产物**，本轮按「单写者模式」严格不触碰，留给后续 session 自行收口或由用户裁决合并顺序。
